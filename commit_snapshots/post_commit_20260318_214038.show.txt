commit 10d0bf882fab7becaa8d57f32ace4d22f7a3c424
Author: Administrator (automated commit) <admin@example.com>
Date:   Wed Mar 18 21:40:25 2026 +0700

    fix: prevent Starting page split undefined crash
---
 commit_snapshots/pre_commit_20260318_213937.diff   | 42 ++++++++++++++++++++++
 .../pre_commit_20260318_213937.status.txt          |  2 ++
 src/app/pages/Starting.tsx                         | 17 +++++++--
 3 files changed, 59 insertions(+), 2 deletions(-)

diff --git a/commit_snapshots/pre_commit_20260318_213937.diff b/commit_snapshots/pre_commit_20260318_213937.diff
new file mode 100644
index 00000000..668f0689
--- /dev/null
+++ b/commit_snapshots/pre_commit_20260318_213937.diff
@@ -0,0 +1,42 @@
+´╗┐diff --git a/src/app/pages/Starting.tsx b/src/app/pages/Starting.tsx
+index cbce4792..d63558ca 100644
+--- a/src/app/pages/Starting.tsx
++++ b/src/app/pages/Starting.tsx
+@@ -36,6 +36,19 @@ interface TaskCatalogItem {
+   productUrl: string;
+ }
+ 
++function getPrimaryLabel(value: string | null | undefined, fallback = 'Product'): string {
++  if (typeof value !== 'string') {
++    return fallback;
++  }
++
++  const normalized = value.trim();
++  if (!normalized) {
++    return fallback;
++  }
++
++  return normalized.split(',')[0];
++}
++
+ // Starting page - Product submission platform with commission tracking
+ export default function Starting() {
+   const navigate = useNavigate();
+@@ -280,7 +293,7 @@ export default function Starting() {
+                 <img
+                   key={slide.id}
+                   src={slide.image}
+-                  alt={slide.name.split(',')[0]}
++                  alt={getPrimaryLabel(slide.product)}
+                   className="max-h-[180px] max-w-[200px] w-full object-contain"
+                 />
+               </div>
+@@ -346,7 +359,7 @@ export default function Starting() {
+                     <div key={index} className="flex items-center gap-3 bg-white/20 rounded p-2">
+                       <img src={product.image} alt={product.name} className="w-12 h-12 object-contain rounded" />
+                       <div className="flex-1">
+-                        <p className="text-white text-sm font-semibold line-clamp-1">{product.name.split(',')[0]}</p>
++                        <p className="text-white text-sm font-semibold line-clamp-1">{getPrimaryLabel(product?.name)}</p>
+                       </div>
+                       <span className="text-white font-bold">${product.price.toFixed(2)}</span>
+                     </div>
diff --git a/commit_snapshots/pre_commit_20260318_213937.status.txt b/commit_snapshots/pre_commit_20260318_213937.status.txt
new file mode 100644
index 00000000..9b99544a
--- /dev/null
+++ b/commit_snapshots/pre_commit_20260318_213937.status.txt
@@ -0,0 +1,2 @@
+´╗┐ M src/app/pages/Starting.tsx
+?? commit_snapshots/pre_commit_20260318_213937.status.txt
diff --git a/src/app/pages/Starting.tsx b/src/app/pages/Starting.tsx
index cbce4792..d63558ca 100644
--- a/src/app/pages/Starting.tsx
+++ b/src/app/pages/Starting.tsx
@@ -36,6 +36,19 @@ interface TaskCatalogItem {
   productUrl: string;
 }
 
+function getPrimaryLabel(value: string | null | undefined, fallback = 'Product'): string {
+  if (typeof value !== 'string') {
+    return fallback;
+  }
+
+  const normalized = value.trim();
+  if (!normalized) {
+    return fallback;
+  }
+
+  return normalized.split(',')[0];
+}
+
 // Starting page - Product submission platform with commission tracking
 export default function Starting() {
   const navigate = useNavigate();
@@ -280,7 +293,7 @@ export default function Starting() {
                 <img
                   key={slide.id}
                   src={slide.image}
-                  alt={slide.name.split(',')[0]}
+                  alt={getPrimaryLabel(slide.product)}
                   className="max-h-[180px] max-w-[200px] w-full object-contain"
                 />
               </div>
@@ -346,7 +359,7 @@ export default function Starting() {
                     <div key={index} className="flex items-center gap-3 bg-white/20 rounded p-2">
                       <img src={product.image} alt={product.name} className="w-12 h-12 object-contain rounded" />
                       <div className="flex-1">
-                        <p className="text-white text-sm font-semibold line-clamp-1">{product.name.split(',')[0]}</p>
+                        <p className="text-white text-sm font-semibold line-clamp-1">{getPrimaryLabel(product?.name)}</p>
                       </div>
                       <span className="text-white font-bold">${product.price.toFixed(2)}</span>
                     </div>
