commit c3140a4a5fd47c9331752445d75b9cc5538f0e2c
Author: Administrator (automated commit) <admin@example.com>
Date:   Wed Mar 18 22:24:27 2026 +0700

    fix: suppress optional 404 toasts for limited admin dashboard
---
 commit_snapshots/pre_commit_20260318_222416.diff   | 42 ++++++++++++++++++++++
 .../pre_commit_20260318_222416.status.txt          |  2 ++
 src/app/pages/Admin.tsx                            | 17 +++++++--
 3 files changed, 59 insertions(+), 2 deletions(-)

diff --git a/commit_snapshots/pre_commit_20260318_222416.diff b/commit_snapshots/pre_commit_20260318_222416.diff
new file mode 100644
index 00000000..7cb55f6a
--- /dev/null
+++ b/commit_snapshots/pre_commit_20260318_222416.diff
@@ -0,0 +1,42 @@
+´╗┐diff --git a/src/app/pages/Admin.tsx b/src/app/pages/Admin.tsx
+index f485d9c7..1d78a04f 100644
+--- a/src/app/pages/Admin.tsx
++++ b/src/app/pages/Admin.tsx
+@@ -428,6 +428,15 @@ export default function Admin() {
+     onMessage: options?.onMessage,
+   });
+ 
++  const isNotFoundError = (error: unknown): boolean => {
++    if (!(error instanceof Error)) {
++      return false;
++    }
++
++    const message = error.message.trim().toLowerCase();
++    return message.includes('(404)') || message.startsWith('404 ');
++  };
++
+   const handleStartVipInlineEdit = (vip: VipLevelConfig) => {
+     setEditingVipLevel(vip.level);
+     setVipDraft({
+@@ -449,7 +458,9 @@ export default function Admin() {
+       setVipConfigurations(tiers.length > 0 ? tiers : defaultVipConfigurations);
+     } catch (error) {
+       setVipConfigurations(defaultVipConfigurations);
+-      handleAdminRequestError(error, 'Failed to load VIP configuration');
++      handleAdminRequestError(error, 'Failed to load VIP configuration', {
++        suppressToast: isNotFoundError(error),
++      });
+     } finally {
+       setVipConfigLoading(false);
+     }
+@@ -506,7 +517,9 @@ export default function Admin() {
+       setRewardsConfig(config);
+     } catch (error) {
+       setRewardsConfig(defaultRewardsConfig);
+-      handleAdminRequestError(error, 'Failed to load rewards configuration');
++      handleAdminRequestError(error, 'Failed to load rewards configuration', {
++        suppressToast: isNotFoundError(error),
++      });
+     } finally {
+       setRewardsConfigLoading(false);
+     }
diff --git a/commit_snapshots/pre_commit_20260318_222416.status.txt b/commit_snapshots/pre_commit_20260318_222416.status.txt
new file mode 100644
index 00000000..1b1ef227
--- /dev/null
+++ b/commit_snapshots/pre_commit_20260318_222416.status.txt
@@ -0,0 +1,2 @@
+´╗┐ M src/app/pages/Admin.tsx
+?? commit_snapshots/pre_commit_20260318_222416.status.txt
diff --git a/src/app/pages/Admin.tsx b/src/app/pages/Admin.tsx
index f485d9c7..1d78a04f 100644
--- a/src/app/pages/Admin.tsx
+++ b/src/app/pages/Admin.tsx
@@ -428,6 +428,15 @@ export default function Admin() {
     onMessage: options?.onMessage,
   });
 
+  const isNotFoundError = (error: unknown): boolean => {
+    if (!(error instanceof Error)) {
+      return false;
+    }
+
+    const message = error.message.trim().toLowerCase();
+    return message.includes('(404)') || message.startsWith('404 ');
+  };
+
   const handleStartVipInlineEdit = (vip: VipLevelConfig) => {
     setEditingVipLevel(vip.level);
     setVipDraft({
@@ -449,7 +458,9 @@ export default function Admin() {
       setVipConfigurations(tiers.length > 0 ? tiers : defaultVipConfigurations);
     } catch (error) {
       setVipConfigurations(defaultVipConfigurations);
-      handleAdminRequestError(error, 'Failed to load VIP configuration');
+      handleAdminRequestError(error, 'Failed to load VIP configuration', {
+        suppressToast: isNotFoundError(error),
+      });
     } finally {
       setVipConfigLoading(false);
     }
@@ -506,7 +517,9 @@ export default function Admin() {
       setRewardsConfig(config);
     } catch (error) {
       setRewardsConfig(defaultRewardsConfig);
-      handleAdminRequestError(error, 'Failed to load rewards configuration');
+      handleAdminRequestError(error, 'Failed to load rewards configuration', {
+        suppressToast: isNotFoundError(error),
+      });
     } finally {
       setRewardsConfigLoading(false);
     }
